const header = () => {
    return (
      <div>
        <h1 className={headerStyles.title}>
          <span>Kalypto</span> App
        </h1>
        <p className={headerStyles.description}>
          Keep up to date with the markets
        </p>
      </div>
    )
  }
  
  export default header